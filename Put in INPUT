//InnerSelf("input");
//Other scripts go above here
const modifier = (text) => {
  // Call Slowburn first to sync the level
  SLOWBURN("input");
  return { text }; 
};
// Don't modify this part (Required)
modifier(text);
